# Makefile for Common Lisp template
.PHONY: build run clean

build:
	@echo "Nothing to build for Lisp. Use your preferred build system."

run:
	sbcl --script src/main.lisp

clean:
	@echo "No build artifacts to clean."
